require "spec_helper"

RSpec.describe TimePeriods do
  it "has a version number" do
    expect(TimePeriods::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
